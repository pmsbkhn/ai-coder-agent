package com.example.library.lending;

/**
 * The enum expected by the tests. It has exactly the same constants as
 * {@code com.example.library.lending.LoanStatus}.
 */
public enum LoanState {
    ACTIVE,
    RETURNED,
    OVERDUE
}
