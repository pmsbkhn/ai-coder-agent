# 3. Escrow owns EscrowId and depends on OrderId; it uses synchronous Java

- Status: Accepted
- Context: system-wide

## Context

Escrow owns EscrowId and depends on OrderId; it uses synchronous Java calls to OrderRepository.

## Decision

Escrow owns EscrowId and depends on OrderId; it uses synchronous Java calls to OrderRepository.

## Consequences

_Recorded as accepted; see the linked Tech Spec / model for effect._
