# 4. IdempotencyKey from shared kernel is stored inside Escrow aggregate to

- Status: Accepted
- Context: system-wide

## Context

IdempotencyKey from shared kernel is stored inside Escrow aggregate to guarantee exactly-once settlement.

## Decision

IdempotencyKey from shared kernel is stored inside Escrow aggregate to guarantee exactly-once settlement.

## Consequences

_Recorded as accepted; see the linked Tech Spec / model for effect._
